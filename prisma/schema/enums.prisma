enum Role {
  USER
  ADMIN
}

enum ReconciliationStatus {
  PENDING
  MATCHED
  DISCREPANCY
}

enum SyncProvider {
  MSC
  QBO
  BLUEPRINT
}

enum SyncStatus {
  PENDING
  SUCCESS
  FAILURE
}

enum CategoryStatus {
  UNCHECKED
  CORRECT
  NEEDS_REVIEW
  FLAGGED
}
